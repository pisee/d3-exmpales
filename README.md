# d3-exmpales

- install
    ```
    $ npm install
    ```

- front
    - ALTER1. using web
        ````
        $ npm run parcel
        ````
    - ALTER2. using electron
        ```
        $ npm start
        ```

- backend
    ```
    $ npm run jserver
    ```